const obj = {
  js: "javascript",
  cpp: "c++",
  rb: "ruby",
  py: "python",
};

// for in loop is used to iterate objects

for (const key in obj) {
  // console.log(key);
  // console.log(obj[key]);
  // console.log(`${key} shorcut is for ${obj[key]}`);
}

const programming = ["a", "b", "c", "d", 8, 55, false, null, undefined];

for (const key in programming) {
  // console.log(key);
  // console.log(programming[key]);
  // console.log(`${key} is the key for ${programming[key]}`);
}
 