<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-400 text-white">
      <div class="form p-10">
        <form action="">
          <input
            class="block px-5 py-2 w-full rounded-md bg-zinc-800"
            type="text"
            placeholder="Title goes here.."
          />
          <textarea
            class="block mt-2 px-5 py-2 w-full rounded-md bg-zinc-800 resize-none"
            placeholder="Write your task details here"
          ></textarea>
          <input
            class="mt-2 bg-blue-400 px-5 py-2 rounded-md"
            type="submit"
            value="Create task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex flex-wrap gap-3">

        <% if(files.length>0){%>
            <% files.forEach(function(val){%>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-3xl tracking-tighter">Some task goes here.</h1>
          <a class="text-blue-500 inline-block mt-3" href="">Read more..</a>
        </div>
           <% }) %>
      <%  } else{%>
           <h2>no tasks yet...</h2>
      <%}%>

        </div>
      </div>
    </div>
  </body>
</html>
